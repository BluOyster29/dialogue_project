<?xml version="1.0" encoding="utf-8"?>

<vxml version="2.1" xmlns="http://www.w3.org/2001/vxml"
      xmlns:voxeo="http://community.voxeo.com/xmlns/vxml">

  <link next="lab1">
  <grammar mode="voice" version="1.0" root="root">
    <rule id="root" scope="public">
       <one-of>
         <item>main menu</item>
	 <item> start over</item>
       </one-of>
    </rule>
  </grammar>
</link>

  <nomatch>
    no match needs something
    <reprompt/>
  </nomatch>
  
  <noinput>
    no input needs something
    <reprompt/>
  </noinput>

  <!-- Data model -->
  <var name="name" />
  <var name="ready2Play" />
  <var name="when"/>
  <var name="isRetrun"/>
  <var name="correct"/>
  <var name="returnDay"/>


  <!-- State machine -->
  <form id="welcome">
    <field name="playerName">
      <prompt>
	
	Welcome to The Altered Beast escape room game!
    What is your name?

      </prompt>
      </field>
      <filled>
      <assign name="name" expr="playerName">
      </filled>
    <field name="readyPlay">
        <prompt>
        Welcome <var expr="playerName"/>,
        Are you ready to play?
        </prompt>
        <filled>
        <assign name="ready2Play" expr="readyPlay"/>
        <if cond="ready2Play" != "yes">
            <prompt> 
            Tough titties mate.
            </prompt>
        <else>
            Ok then lets go!
            <goto next="instructions"/>
        </else>
    </form>
            
  

<menu id="instructions">
  <prompt>
    This is the main menu where you will make some choices. Please say whether you would 
    like to play in easy mode, medium mode, or hard mode. 
  </prompt>
  <choice next="easy">
    Easy mode
  </choice>
  <choice next="medium">
    medium mode
  </choice>
  <choice next="hard">
    hard mode
  </choice>
  
</menu>

</vxml>